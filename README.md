### Config data
`environment_web_socket`: Websocket url  
`debug_mode`: some actions will be handled differently when `true`  
`use_custom_rules`: if an adventure will use the custom difficulty settings from `NeuroFTKCustomHouseRules.json` located in the same folder as the plugin dll. 
